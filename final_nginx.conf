user  nginx;
worker_processes  1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name working-shop.chickenkiller.com;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name working-shop.chickenkiller.com;

        ssl_certificate /etc/letsencrypt/live/working-shop.chickenkiller.com/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/working-shop.chickenkiller.com/privkey.pem;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
